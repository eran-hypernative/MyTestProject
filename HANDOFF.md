# מצפן — מצב העבודה

מסמך העברה לסשן חדש. קרא אותי קודם.

## איפה זה עומד

האפיון: `docs/compass-product-spec.md`. הוא מקור האמת. סעיפים מצוטטים לפיו.

**עובד ובדוק:**
- `app/index.html` — האפליקציה. קובץ יחיד, בלי תלויות. המפה, מגירת הראיות, מד הכיסוי,
  מסך הסקטוריאליות, מנוע המשקולות, עמוד המתודולוגיה, ומסך הקידוד המהיר.
  נטענת ריקה בכוונה. `?demo=1` מציג נתונים בדויים לבדיקת ממשק בלבד.
- `harvest/` — הצינור. `probe` אימת את כל שמונה הישויות מול השרת החי.
- `harvest/test/` — 8 בדיקות על השיוך לפי תאריך. עוברות.

**במטמון כבר:** `KNS_Faction` (16), `KNS_Person` (1188), `KNS_PersonToPosition` (2178).

**טרם נמשך:** `KNS_PlenumVote` (7,557 שורות) ו-`KNS_PlenumVoteResult` (526,483 שורות,
כ-36 דקות, מחולק לקבצים חודשיים).

## הצעד הבא

```
node harvest/harvest.mjs pull --knesset 25     # ממשיך מהמטמון, בטוח להריץ שוב
node harvest/build.mjs --knesset 25
```

`build` ידפיס "מיפוי ערכי הצבעה שנצפה בנתונים". **חייבים לעבור על זה בעיניים**
לפני שמקדדים משהו — סעיף 7.1 דורש זאת ואין enum ב-`$metadata`.

## מה ידוע ופתוח

1. **תור הסקירה יהיה ענק.** 7,557 הצבעות כפול כתריסר סיעות זה כ-90,000 שורות.
   אי אפשר לקדד ידנית. צריך לסנן, אבל **אין להמציא סינון לפני שרואים כותרות אמיתיות.**
   שדות זמינים לסינון: `VoteMethodDesc`, `VoteStatusDesc`, `IsNoConfidenceInGov`.
   הצעד הנכון: להדפיס דגימה של 50 כותרות, להסתכל, ורק אז להחליט.
2. **חמש השאלות הפתוחות בסעיף 12 באפיון טרם הוכרעו.** במיוחד שאלה 2 (הסכם קואליציוני
   מוצהר או בפועל — כרגע מסווג כבפועל) ושאלה 4 (האם ציר משטר ומוסדות נכנס לגרסה ראשונה).
3. **מיקום הבסיס** מוחל כרגע על דלי `actual` בלבד. האפיון לא מכריע. יש לאשר.

## כללים שאסור לשבור

- **אין להמציא נתונים.** לא הצבעות, לא מיקומים, לא קישורים. אם אין נתון, המפה ריקה,
  וזו התצוגה הנכונה לפי סעיפים 3.5 ו-5.4.
- **הקידוד הוא עבודת אדם** (סעיף 7.3). שום סקריפט לא ממלא `direction` או `magnitude`.
  מותר להציע, אבל רק כשמסומן במפורש כהצעה הממתינה לאישור.
- **אין להניח שמות שדות.** הסכימה נבדקת מול `$metadata`. אם משהו לא נמצא — נעצרים
  ומדווחים, לא ממשיכים בשקט.
- **RTL** — סעיף 6 הוא רשימת בדיקה, לא המלצה. כל מספר נעטף ב-`.ltr`, `text-anchor`
  הוא `middle` בלבד, ואין תווי חץ בתוך מחרוזות עברית.

## סביבה

Node 18+. אין תלויות חיצוניות, אין `npm install`.
הענף: `claude/compass-product-spec-ooei3k`.
